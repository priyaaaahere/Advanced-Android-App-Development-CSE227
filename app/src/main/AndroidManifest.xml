<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" /> <!-- for bluetooth -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" /> <!-- for android 12+ -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" /> <!-- for wifi -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" /> <!-- for scanning wifi networks -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CSE227ETP">
        <activity
            android:name="unit1.SignUpMain"
            android:exported="true" />
        <activity
            android:name="unit1.LoginMain"
            android:exported="true" />
        <activity
            android:name="unit2.CanvasMain"
            android:exported="true" />
        <activity
            android:name="unit4.MotionSensorMain"
            android:exported="true" />
        <activity
            android:name="unit4.EnvironmentSensorMain"
            android:exported="true" />
        <activity
            android:name="unit4.PositionSensorMain"
            android:exported="true" />
        <activity
            android:name="unit5.WifiBtAirplaneMain"
            android:exported="true" />
        <activity
            android:name="unit6.WebViewMain"
            android:exported="true" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>